import { SaveChangesDirective } from './save-changes.directive';

describe('SaveChangesDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveChangesDirective();
    expect(directive).toBeTruthy();
  });
});
