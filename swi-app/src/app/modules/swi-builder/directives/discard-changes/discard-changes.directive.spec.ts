import { DiscardChangesDirective } from './discard-changes.directive';

describe('DiscardChangesDirective', () => {
  it('should create an instance', () => {
    const directive = new DiscardChangesDirective();
    expect(directive).toBeTruthy();
  });
});
